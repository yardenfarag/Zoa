{
  "name": "zoa",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w frontend",
    "lint": "npm run lint -w frontend",
    "dev:api": "npm run start:dev -w backend",
    "build:api": "npm run build -w backend",
    "seed:api": "npm run seed -w backend",
    "sync-images:api": "npm run sync-images -w backend",
    "sync-images:api:force": "npm run sync-images -w backend -- --force"
  }
}
